---
name: futures-analyzer
description: 期货心理博弈分析助手：四方资金（产业/主力/投机/散户）心理博弈 + 散户行为预警 + 交易记录实时诊断（本地 Web 应用）。当用户提到 futures-analyzer、期货心理、散户心理、资金博弈、交易诊断、反幻想时触发。
version: "2.0.0"
icon: "♜"
metadata:
  source: "https://github.com/lilaosanxueai/futures-analyzer"
  installed_by: "github-installer"
  installed_at: "2026-08-25T21:00:15.036656"
  synced_at: "2026-09-15T00:10:00"
---

# ♜ 期货心理博弈分析助手（本地 Web 应用）

> 2026-09 彻底重构：去掉全部技术指标 / 资讯 / 宏观日历功能，全面转向
> **四方资金心理博弈 + 散户行为分析 + 交易实时诊断**。

## 核心理念（本应用的世界观）

价格不是图形，是四方资金的博弈结果：

| 参与方 | 行为特征 | 推断依据 |
|---|---|---|
| 🏭 产业（套保盘） | 高位卖保压制、低位买保支撑 | 价格在周期中的位置（分位） |
| 🏦 主力（机构资金） | 进攻/回补/兑现/诱多诱空 | **持仓量 × 价格** 的八状态组合（最硬的证据） |
| ⚡ 投机（短线热钱） | 放量追涨 / 杀跌 / 离场 | 日内量能比 + 短时动能 |
| 🐑 散户（情绪盘） | 追涨杀跌 / 抄底扛单 / FOMO | 行情阶段剧本 + 即时情绪修正 |

分析只回答四个问题：
1. 四方资金此刻在做什么？
2. **散户在想什么、会怎么操作？**（核心输出）
3. 主力会怎么利用散户的情绪？
4. 该**顺应**什么（大势 / 资金方向 / 品种现状 / 情绪）？

## 使用方法

- **start.bat**：双击启动（防重复启动、就绪后自动开浏览器，控制台窗口关闭即停止服务）。
- ⚠️ 桌面「期货开关.vbs」已应用户要求删除，**勿重建**（同 ai-treehole 桌面开关先例）。
- 浏览器打开 `http://127.0.0.1:8300`
- AI 对话：右上角「⚙ 设置」填智谱 / DeepSeek / 自定义端点的 API Key
- 注意：仅供研究学习，不构成投资建议，无下单功能

## 功能地图

### 1. 工作台
- 自选博弈看板：最新价 / 涨跌 / **今日持仓变化**（实时 vs 昨收盘）/ 资金方向徽章（增仓上行等八状态）/ **散户陷阱指数**（0-100 进度条）
- 外盘大势锚（WTI / 布油 / 黄金 / 美元指数）
- 预警事件流：价格异动（AI 资金视角解读）/ 博弈雷达（行情阶段切换、资金定性翻转、陷阱升高）/ 交易诊断预警 / 移动止盈提示
- AI 反幻想教练对话（快捷问法：散户在想什么 / 主力在做什么 / 现在能进场吗 / 拆穿我的幻想）

### 2. 品种博弈（点击自选行进入）
- 行情阶段横幅：9 阶段剧本（高位加速 / 高位增仓滞涨 / 高位回落 / 趋势上行 / 区间震荡 / 趋势下行 / 低位阴跌 / 低位恐慌 / 低位放量企稳）
- 四方博弈卡片：产业 / 主力 / 投机各给立场+证据+意图+置信度；**散户卡片给"在想什么 + 可能操作 + 陷阱指数"**
- 危险行为警告框：此刻对散户最危险的具体行为
- 反幻想顺应框：要拆穿的幻想 + 主力剧本 + 顺应结论
- 价格位置：60 日区间分位 + 近 90 日收盘折线（仅示位置）+ 20 日中枢（大势分界）
- 资金博弈证据：日线八状态 / 20 日持仓趋势 / 量能比 / 日内八状态 / VWAP 偏离 / 持仓 15 分变化
- 「AI 深度解读」「盘中快评」按钮

### 3. 交易诊断（个人交易记录 + 实时问题暴露）
- **记一笔**：保存即检，问题当场暴露（不拦截只预警）
- 持仓盯防（60 秒巡检 + 飞书推送）：价格穿止损未离场 = 致命预警
- 诊断规则：D01 无止损 / D02 扛单越损 / D03 止损窄于噪音 / D04 单笔风险超标 / D05 日内超频 / D06 报复性交易 / D07 逆资金方向 / D08 追涨杀跌 / D09 左侧抄底摸顶 / D10 高相关品种堆仓 / D11 连亏未停 / D12 日亏停手线 / D13 持亏砍盈
- 行为统计：胜率 / 盈亏比 / **散户病计数**（无止损·扛单·报复交易）
- 移动止盈：浮盈达标自动激活追踪线，触发提示进事件流+飞书

### 4. 交易心得
- 心得 CRUD + 飞书云文档同步
- AI 复盘：交易记录 + 心得 + 对话 → **行为模式画像 / 幻想清单 / 做对的事 / 改进建议**（可存飞书）

### 5. 晨/夜报（心理博弈简报）
- 8:50 晨报 / 20:50 夜报自动生成推飞书：市场情绪概览 / 分品种博弈要点 / **今日纪律（不做清单）**

## AI 人设（SYSTEM_PROMPT 要点）

「反幻想交易教练」：
- **禁止**任何技术指标语言（均线金叉死叉/MACD/KDJ/RSI/布林/波浪；20 日均线仅作大势分界参考）
- **禁止迎合**：用户想抄底/扛单/重仓时直接指出这是散户幻想及其代价
- 框架：品种现状 → 四方在做什么 → 散户怎么想会怎么做 → 主力怎么利用 → 顺应结论
- 所有判断给具体数值；数据缺失明说

## 技术架构

```
app.py          FastAPI 后端（行情层 / AI 层 / 盯盘与诊断循环 / 飞书 / 晨夜报）
psychology.py   心理博弈引擎（纯计算）：行情阶段判定 / 四方推断 / 剧本库 / 警告生成
static/         前端：index.html + style.css + js/{base,views,chat}.js
trades.json     交易记录（用户数据）
trader_profile.json  交易者画像（风格/教训/规则，注入 AI）
```

- 数据源：AkShare + 新浪 hq 直连（价格 / 成交量 / **持仓量**）
- **日线一律用主连连续合约**：具体月份合约的持仓是生命周期滚仓（新主力从 0 滚到满仓），
  跨日比较会把换月误判成资金进出；连续合约持仓为全市场口径、时间连续
- AkShare 单线程网关（V8 引擎只能初始化一次）+ 18s 超时 + 90s 熔断降温
- AI 多服务商自动降级（智谱 / DeepSeek / 自定义），熔断 10 分钟沉底
- 后台循环：盯盘 30s / 移动止盈 30s / 博弈雷达 90s / 交易诊断 60s / 晨夜报 120s / 自检 600s

## 安装来源

- **GitHub**: [lilaosanxueai/futures-analyzer](https://github.com/lilaosanxueai/futures-analyzer)
- **版本**: main@de98481（v2.0.0，2026-09-15 同步）
- **语言**: Python 3.12（FastAPI + AkShare + httpx）
- **本地路径**: `C:\Users\10166\.agents\skills\futures-analyzer`
- **虚拟环境**: `.venv`（依赖已装：akshare 1.18.94 / fastapi 0.141.1 / pandas 3.0.5 / uvicorn 0.52.4 / pypinyin 0.55.0，v2 依赖无变化，直接复用）
- 启动：`.venv\Scripts\python.exe app.py` 或双击 start.bat → http://127.0.0.1:8300

## 本地同步记录（2026-09-15，v0826o → v2.0.0）

上游在本地 v0826o（=3db246d）之后合并了 main 分支「超短线体系」并**彻底重构为心理博弈分析助手**，本次全量同步至 de98481：

1. **文件级同步**：14 个文件哈希逐一校验与上游 HEAD 一致；删除 indicators.py（技术指标随重构移除）与 static/app.js（拆分为 js/{base,views,chat}.js）；清理 __pycache__。
2. **跳过 `期货开关.vbs`**：上游合并时该文件被带回，但用户明确要求删除勿重建，本地不复制。
3. **运行数据保留**：config.json（API Key/飞书凭证原样兼容）、notes.json、morning_report.json；旧纪律日志 `discipline_log.json` 保留在本地（v2 改用 trades.json，旧文件仅为历史存档，新版不读取）。
4. **v1 完整备份**：`C:\Users\10166\.agents\backup\futures-analyzer-v0826o\`（含旧版全部代码 + 48KB 历史修复记录版 SKILL.md；v1 的纪律引擎 R001-R014 / K线 TradingView 交互 / 六套皮肤等已随上游重构移除，需要时可从备份恢复）。
5. guardian.bat / guardian_hidden.vbs 为上游新增服务守护（崩溃自动重启），文件已就位但未注册开机自启，按需手动启用。

## 本地功能增强（2026-09-15，v2.0.0+）：宏观要闻辅助参考

应用户要求把 v1 消息面「高度精简后作为辅助参考」移植回 v2（继承 v0825t 用户调优的白名单方案）：

1. **`/api/news` + `_news_context(n)`**（app.py）：新浪全球快讯单源（`ak.stock_info_global_sina` 走 call_ak 网关），严格白名单过滤——trump（特朗普/白宫/关税…）+ mideast（伊朗/以色列/霍尔木兹/OPEC…）两组词表原样继承，新增 fed（美联储/降息/非农/CPI…，贵金属/有色夜盘第一驱动）；TTL 300 秒、上限 30 条、失败静默（辅助信息不阻塞主链路）。
2. **注入四个 AI 调用点**：AI 对话上下文（5 条）、品种深度解读与盘中快评（各 3 条）、晨/夜报（5 条，情绪概览段要求结合要闻）。
3. **铁律约束**：SYSTEM_PROMPT 新增「宏观要闻仅作辅助参考——只解释散户情绪来源与隔夜跳空风险，禁止用消息面给方向背书」，保持反幻想教练人设不跑偏。实测：AI 原文引用两条要闻后明确指出"这两条消息不改变任何品种的价量持仓结构，光靠新闻开单是为幻觉买单"。
4. **前端**：工作台外盘锚下方新增「宏观要闻」紧凑卡（🇺🇸/🌍/🏦 分组图标 + 时间 + 单行标题省略号，最高 150px 内滚动，5 分钟轮询，标题 esc 转义）。
5. 注意：白名单命中少是设计使然（整条快讯流常只剩 0~5 条高冲击事件），不是故障。

## 本地功能增强（2026-09-15 二）：品种要闻——只留明确影响行情的

在宏观要闻之上新增品种层（应用户要求"关注品种的相关信息，保留明确对行情产生影响的"）：

1. **`_variety_news(symbol)` + `/api/news?symbols=`**（app.py）：东财产业新闻单源（`ak.stock_news_em`，搜索词表继承 v1 dfd545d 的 VARIETY_SEARCH，40+ 商品品种），**双重过滤**：① 股市噪音词剔除（个股/财报/评级类）；② 影响词**只认标题**（供给事件/政策储备/库存仓单/需求订单/价格动作五类 ~45 词）——事实性标题会直接点名"库存去库/减产/订单回暖"，评论性标题（"上有压制下有支撑"）不会，借此把行情评论挡在门外。每品种 10 分钟缓存，失败静默。
2. **实测过滤质量**（RB/SA/MA/AU 四品种）：粗滤（标题+摘要）会混入期货日报行情评论、公司供应链新闻；收紧为标题命中后只剩 3 条真实供需事实——甲醇港口库存持续去库（中财期货）、纯碱供应及库存绝对量高位（正信期货）、玻璃终端订单无回暖纯碱刚需弱势（瑞达期货），评论/公司/航运新闻全部滤除。
3. **注入**：AI 对话（选中品种 2 条）、品种深度解读与盘中快评（各 2 条）、晨/夜报（自选每品种 1 条共 6 条）；均标注「供需/库存/政策事实，供产业方立场与情绪解释，不作方向依据」。
4. **实测 AI 链路**（SA0）：AI 原文引用三条纯碱供需事实 + 标注"要闻未给具体吨数无法量化" + 明确"这两条要闻不构成做空依据，方向只由持仓说话"，并用增仓下行 +46.2% 的实时持仓数据戳穿"利空出尽"叙事（公平世界幻觉）——辅助参考定位完全落地。
5. **前端**：工作台要闻卡升级为双层——品种条目（带品种徽章，如「纯碱」）在前、宏观条目（🇺🇸/🌍/🏦）在后，最多 10 行 172px 内滚动，5 分钟轮询时自动带上自选品种。

## 故障修复（2026-09-15 三）：要闻轮询堵死品种目录——下拉框无品种

用户报「下拉框失效，没有可选品种」（自选行情正常）。根因：要闻抓取（stock_info_global_sina + 每品种一次 stock_news_em）走了与行情/目录共享的 `call_ak` V8 单线程队列——页面一打开要闻先排进 5+ 个调用，`/api/main-list` 目录抓取被堵在后面；要闻源超时还会累积共享熔断计数（连续 3 次即全局停发 90 秒），目录直接快速失败并缓存空结果，下拉永久为空（旧前端也不会重试）。

1. **侧路执行器 `_call_ak_side`**（app.py）：新增独立 `_SIDE_EXECUTOR`（max_workers=2）——新浪快讯与东财搜索是纯 requests+pandas 接口，**不依赖 py_mini_racer/V8**，与主队列隔离绝对安全；15 秒超时、不重试、不触发熔断，失败由调用方的负缓存兜底（空结果缓存满 TTL，天然退避）。两处新闻抓取全部切换。
2. **前端目录自动重试**（base.js）：`loadDirectory` 目录为空时每 60 秒重试（适配后端目录失败 5 分钟退避），目录恢复后下拉自动填充。
3. **实测**（8301 旁路实例并发验证）：要闻 5 连发抓取进行的同时，`/api/main-list` 2.1 秒完整返回 ~90 个品种（旧代码此时会被堵/熔断）；要闻缓存命中 1.0 秒。
4. **教训**：给共用单线程队列加新调用方前，先想清楚它会不会抢核心路径（目录/行情）的队头位、失败会不会污染共享熔断计数——非 V8 依赖的接口一律走侧路。

## 账户接入方案 C（2026-09-15 四）：交割单导入 + 历史交易 AI 复盘

应用户选择实施（A=miniQMT / B=CTP 直连 暂缓，凭证门槛待后续；C 零门槛当天可用）：

1. **`POST /api/trades/import`**（app.py）：粘贴 CSV/制表符成交流水（Excel 直接 Ctrl+C/V）。列名别名兼容各家期货公司（成交日期/时间/合约/买卖/开平/成交价/成交量/手续费）；方向兼容「买/卖 + 开仓/平仓/平今/平昨」与「买开/卖平」复合写法；日期兼容 2026-09-14 / 2026/9/14 / 20260914。**FIFO 配对**：卖平=平多、买平=平空，部分平仓自动拆手数、跨开仓批次按先进先出，剩余未平仓转为当前持仓；id 确定性生成，重复导入自动去重。dry_run=1 仅预览。记录带 `source:"import"`、`contract`（具体月份合约）、`fees`、`hold_min`（持时分钟）。
2. **文件编码在前端处理**（views.js）：TextDecoder utf-8 严格失败回退 gbk（浏览器原生），走 JSON 文本接口——**不用 multipart**（UploadFile 端点在无 python-multipart 的环境会让整个应用启动崩溃，实测踩坑后移除）。
3. **`POST /api/ai/trade-review`**：真实数据聚合（胜率/盈亏比/最长连亏/方向偏好/分品种点数/**盈利单 vs 亏损单平均持时对比**（处置效应硬证据）/亏损后 30 分钟内再开仓计数/手续费合计）+ 最近 60 笔明细 + 当前持仓 → 反幻想教练五段式报告（行为模式举证/人性定律归因/品种方向陷阱/做对的事/可执行建议）。样本 <3 笔明确拒绝并引导先导入。空 wins/losses 已做除零保护（实测 4 笔全盈利样本抓出该 bug）。
4. **实测**：7 条流水（部分平仓+隔夜+跨批次 FIFO+留仓）→ 4 闭环 + 1 持仓，点数/持时全对；制表符+买开卖平+斜杠日期+小写合约格式兼容通过；重复导入 added=0；AI 报告正确指出"胜率 100% 是零亏损样本的统计假象"、"未设止损持仓=损失厌恶+控制幻觉"、小样本连胜警惕近因外推。
5. **前端**：交易日志卡新增「📥 导入交割单」（文件/粘贴 → 解析预览表格 → 确认导入）与「🧠 历史复盘」（时间范围+品种过滤 → Markdown 报告 → ☁ 存飞书复用《AI 复盘报告》文档）。
6. 已知限制：配对仅限单次导入批次内（分段导入时边界持仓无法跨批配对，预览会提示"平仓无对应开仓"）；交割单无止损/目标信息，导入记录 stop_points 为空（诊断引擎按"无止损"口径处理属预期）。

## 全盘扫描与优化（2026-09-15 五）

自动化扫描（幽灵 ID / 死函数 / 路由清单 / 前后端 API 比对）+ 人工核实 + 修复：

1. **扫描结论**：前端无幽灵 ID（psychChart/psychAiCard 系动态创建）；`/api/monitor/test` 用 `@app.api_route` 注册（扫描正则漏报，实际存在）；前端引用的 API 后端全覆盖。**唯一真死代码 `_llm_json`（54 行）已删除**——心理重构移除结构化判定后无调用方。
2. **chat.js 存档审计**：v2 对话存档仅 {role,content,ts}（无图片功能），错误气泡 textContent 渲染且不入存档不入 AI 上下文——v1 的两个历史坑（图片撑爆 localStorage、错误污染语料）在 v2 天然不存在。
3. **五处串行 await 并行化**（本轮主要优化）：
   - `/api/news?symbols=`：逐品种串行（8 品种最坏 8×15s 超时叠加）→ `asyncio.gather` 并行，实测 4 品种 0.57s；
   - AI 对话上下文、品种深度解读、盘中快评：宏观+品种要闻双源串行 → gather 并发（缓存未命中时等待时间减半）；
   - 晨夜报：自选品种要闻逐个串行（首次生成最坏 8×15s）→ gather 并行；
   - 侧路线程池 2→4 workers（新闻源为新浪/东财两家独立服务，4 并发礼貌且够用）。
4. **冒烟**：17 个端点全部 200（行情 0s / 要闻 0.57s / 快评 11.7s 为 LLM 耗时 / 目录冷启 4.8s），三 JS 语法校验通过，模块导入 45 条路由注册正常。

## 历史交易盘面回放（2026-09-15 六）：复盘从行为统计升级到决策质量审计

应用户要求实现（"分析历史交易数据时拉取相关交易日的品种走势"）。核心：`_replay_tag`（app.py）用主连日线回溯每笔交易的入场时刻快照与持有期轨迹：

1. **数据**：`_daily_bars_for(symbol)` 走现有 get_daily（主连连续合约，持仓量全市场口径，sina 全量历史实测 1643 根/6.8 年）。**交易日归属**：20 点后成交归下一根日线（`_trade_day_idx`，夜盘正确落位）。
2. **入场快照**：60 日分位、距 MA20 偏离、5 日资金状态（增仓上行/增仓下行/减仓上行/减仓下行，持仓量×价格）、当日区间位置。**行为标签**（⚠）：追涨杀跌位（多入当日区间≥78% 且日涨 / 空入 ≤22% 且日跌）、逆势抄底摸顶（逆 5 日价格趋势 + 极端分位）、逆 5 日资金。
3. **持有期 MFE/MAE**：方向化计算——多头看最高/最低、**空头看最低/最高（方向取错会把浮盈浮亏互换，实测 SA 空单"浮盈极值无"暴露后修复并补双向单测）**；浮盈兑现率 = 实际结果 ÷ 最大浮盈（低=该赚没赚到）；拿早判定 = 兑现 <50% 且出场后 3 根日线继续有利方向。
4. **聚合注入 trade-review**：追涨杀跌 X/N、逆势抄底 X/N、兑现率均值、扛单深度均值、拿早笔数；逐笔明细带 [回放标注]；提示词新增「决策质量审计」一节。
5. **两个实测抓出的坑**：①合约口径校验——具体月份合约价格与主连日线背离超 ±10%（换月期/非主力）时放弃回放（宁缺毋滥），否则冒出"236% 分位"垃圾值；②展示语义——区间位置夹 0-100、浮盈/浮亏越界侧显示"无"、浮亏均值只统计负值。
6. **验收**：合成数据双向单测精确断言通过（多 MFE+5/MAE-2、空 MFE+2/MAE-5）；端到端报告输出关键洞察——"逆 5 日资金 3/4 笔贡献 80% 利润，但 SA 扛 -53 浮亏为最终盈利 2.4 倍（幸存者偏差）"、"RB 兑现 60% 该赚没赚"、"4 笔全胜是零亏损样本假象"。
7. 局限：日线粒度近似（日内 MFE/MAE 无法精确）；编造价格与真实日线背离时引擎诚实报告缺失而非编造。

## 日内走势接入（2026-09-15 七）：实时分时图 + 复盘日内精度

应用户要求（"日内的实时走势，可以获取吗"）。分钟数据实测能力（sina，各 1023 根滚动窗口）：1 分钟 ≈3 天、5 分钟 ≈3 周、15 分钟 ≈2 个月、30 分钟 ≈4 个月、60 分钟 ≈8.5 个月。

1. **`GET /api/intraday/{symbol}`**：1 分钟线重建当前交易时段（含昨夜夜盘——凌晨 4 点前归属昨日 21:00 起的时段；边界处理见 `_in_session`），累计成交量加权均价线 + fetch_quote 的昨结基准/最新价/时间。实测日盘 255 根（21:01~11:30）。
2. **品种博弈页「日内分时走势」卡片**（views.js `loadIntradayChart`）：canvas 绘制价格线（红涨绿跌 vs 昨结）+ 黄色均价线 + 昨结虚线基准 + 右缘最新价标签 + 首中尾时间轴；停留该页时每 60 秒自动刷新（离开即停）。
3. **复盘 MFE/MAE 日内精度**：trade-review 预载 15 分钟线（(ms,high,low) 索引），持有窗口在覆盖范围内的交易用分钟级高低点算浮盈/浮亏极值（标注「15分钟精度」），老交易自动回退日线近似。实测报告正确带出精度标注，且 AI 批判性指出"兑现率 210%>100% 属口径失真"（编造出场价 vs 真实主连——真实交割单不会出现）。
4. 已知：sina 1 分钟线下午时段偶有滞后（实测 14:40 时末根停在 11:30），价格标签用 hq 实时价兜底；分时图为轻量自绘，非 TradingView 级。
5. 坑：Edit 插入代码时误吞了相邻的 `@app.get("/api/intl")` 装饰器（路由消失、函数变孤儿）——大段插入后必须核对被插入点两侧的装饰器完整性（本次靠 Read 及时发现）。

## 全盘梳理第二轮（2026-09-15 八）：消 bug + 加倍优化

扫描 v2（幽灵 ID/死函数/API 对齐/innerHTML 未转义插值）全部干净；本轮修复与优化均有单测或冒烟验证：

1. **`load_config` mtime 缓存**：原先每次调用读盘+JSON 解析——它是全应用最高频函数（监控/诊断/LLM 每次都调）。按文件 mtime 缓存已解析结果，命中返回深拷贝（调用方会改后回存，不能共享引用；save 改 mtime 自动失效）。实测 200 次调用 3.2ms，深拷贝隔离验证通过。
2. **交割单时间兼容加固（`_imp_dt`）**：个位小时 `9:05`、斜杠个位日期 `2026/9/4` 此前解析失败会让整条成交丢失日期。6 用例矩阵单测通过。
3. **分时图两处修复**：①刻度范围纳入 hq 实时价（分钟线滞后时最新价圆点会画出边界）；②交易时段重建改「锚定夜盘首根」（前一根非 21 点档的 21 点档 K 线）——修复周一边界（周一夜盘=周五 21:00，按日历找昨日会漏）与锚到 21:59 漏头一小时两问题，非夜盘品种回退日历逻辑。实测锚点 21:01、255 根完整时段。
4. **持仓行情并行补拉**：`get_trades` 与 `_diagnosis_scan` 原先逐持仓串行 `await fetch_quote`（N×0.5s）→ 缓存未命中品种 `asyncio.gather` 并行预取。诊断扫描保留 price=None 仍诊断的语义（D01/D07 不依赖现价）。
5. **本轮自查出的自家 bug**（都没逃过验证关）：重构段锚时漏了 `todays` 赋值（冒烟 500 抓回）；诊断并行化初版 `continue` 跳过了无需现价的诊断项（自查纠正）。
6. 冒烟：17 端点全部 200；三 JS 语法通过。

## 散户缺陷强化（2026-09-15 九）：引擎动态定律 + 个人缺陷画像闭环

应用户要求"针对散户存在的问题和缺陷，强化升级"。三层：

1. **引擎层（psychology.py）**：人性定律 8→11 条——新增**沉没成本**（扛单/摊平的心理根源）、**过度自信**（连胜加仓）、**羊群效应**（追最热的品种）。定律激活从「按阶段静态」升级为「阶段基线 + 盘面信号动态」：放量+日内高位+急涨 → 追加 fomo/herding 且陷阱指数 +5；放量+日内低位+急跌 → herding/loss_aversion；深跌区（60 日 ≤25% 分位且 20 日跌 >3%）→ sunk_cost；高位+持仓拥挤（≥80% 分位）→ overconfidence。单测验证：无信号时保持基线不误触发。
2. **个人层（app.py `_flaw_profile`）**：从真实交易记录检测**该交易者**的 7 类缺陷（无止损开仓/穿损扛单/持亏砍盈/盈亏比倒挂/报复性交易/频繁交易/连胜后加仓），每项含证据数字、严重度 0-100、**解药**（具体纪律规则）；样本 <3 笔返回空（不拿两笔交易贴标签）。`/api/flaw-profile` 端点 + `_flaw_context()` 注入三处：AI 对话 system（非 light）、品种深度解读、晨夜报（今日纪律段逐条点名缺陷）。
3. **前端**：交易诊断页新增「🐑 你的散户缺陷画像」卡——严重度进度条（复用 trap-bar 样式）+ 证据 + 解药，随统计刷新，空数据引导导入。
4. 实测：构造覆盖全部 7 缺陷的 10 笔样本 → 全部命中且按严重度排序（持亏砍盈 50 / 倒挂 50 / 报复 48 / 无止损 30 / 频繁 25 / 穿损 24 / 连胜加仓 12）；动态激活三场景（追涨温床/静态基线/深跌沉没成本）断言通过。测试中的两个"失败"均为测试自身问题（样本未构造出两连胜时序、陷阱指数期望值算错），引擎行为正确。

## 开仓闸门（2026-09-15 十）：从源头堵散户高发亏钱模式

应用户要求"从源头堵住接飞刀/亏损加仓/止损犹豫/幻想反弹/逆势而为"。设计哲学：**不剥夺开仓自由，但强制二次确认并留违规标记**——摩擦即防线。

1. **`_entry_gate`（app.py）六类致命拦截器**，开仓瞬间用实时博弈快照+持仓判定：
   - **G1 接飞刀**：做多 + 下跌阶段（低位阴跌/恐慌/趋势下行/高位回落或增仓下行）+ 15 分钟仍急跌 ≤-0.25%
   - **G1b 恐慌末端追空**：做空 + 低位恐慌/阴跌 + 贴日内最低（≤15% 分位）+ 急跌 ≤-0.4%（接飞刀的镜像）
   - **G2 幻想反弹**：做多 + 60 日 ≤22% 分位 + 20 日跌 ≥3%（深跌区赌 V 型反转）
   - **G3 逆资金方向而为**：做多 vs 增仓下行 / 做空 vs 增仓上行
   - **G4 追涨 FOMO**：做多 + 贴日内高点（≥85% 分位）+ 15 分钟急涨 ≥0.4%
   - **G6 亏损加仓（摊平）**：同品种同方向已有浮亏持仓再加仓（不需快照，只看持仓与现价；盈单加仓不误伤）
2. **force 流程**：`POST /api/trades` 先跑闸门——有拦截且未 force → 返回 `{blocked, blockers}` **不入库**；前端红色风险确认框（逐条证据+解药），「放弃」或「我知道风险仍要开仓（记录违规）」；force=1 通过后记录带 `violation` 标记，交易日志行显示 🚫（悬浮显示违规明细）。
3. **止损犹豫分级（D02 升级）**：越损 1.1×=「止损犹豫」（每等一分钟主动权少一分）；**2×=「深度扛单」**（决策权已完全交给对手盘）——两级文案递进，实时盯防+飞书。
4. **缺陷画像第 8 项「无视拦截强行开仓」**（权重 1.3，最高优先暴露）：闸门是统计上最亏钱模式的集合，每次 force 都是为「我知道我在干什么」的幻觉付费。
5. **实测**：单测 9 场景全过（六类拦截/干净放行/盈单加仓不误伤/画像计入/D02 分级）；线上闭环——真实市场恰处下跌+增仓下行，SA0 做多被 G1+G3 精确拦截（实时数据命中）、二次加仓三重拦截（G1+G3+G6）、force 后 violation 完整记录。画像为空属 ≥3 笔样本门槛的设计行为（单测已覆盖该路径）。

## 查漏补缺（2026-09-15 十一）：闸门留痕 + 违规成绩单

1. **闸门拦截日志（gate_log.json）**：被拦下（放弃开仓）的尝试留痕 {ts/symbol/direction/codes}，保留最近 200 条。`/api/flaw-profile` 返回 `blocked_count`；缺陷卡顶部显示「🛡️ 闸门已拦下 N 次开仓尝试（防线在干活）」——正反馈（被拦=省下的手续费与亏损）；`_flaw_context` 同步带战果行，AI 会肯定该行为。
2. **违规成绩单（trade-review）**：聚合新增「🚫 违规对比：强行通过闸门的单 X 笔/胜率/累计点数 vs 听劝的正常单」——用数字回答"强行通过到底亏不亏"；逐笔明细带 🚫强行标记。实测对比行：强行单 2 笔胜率 0% 累计 -35 点 vs 正常单 2 笔胜率 100% 累计 +50 点。
3. **行为统计扩容**：`/api/trades/stats` behavior 增加 forced（强行开仓数）与 blocked（被拦数），前端散户病计数行同步展示。
4. 回归扫描：幽灵 ID 无、死代码无（路由函数为误报）、三 JS 语法通过。
