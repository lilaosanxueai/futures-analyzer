---
name: futures-analyzer
description: 期货实时分析助手：准实时行情 + K线 + 技术信号 + AI 盯盘/晨报/对话（本地 Web 应用）。当用户提到 futures-analyzer、期货分析、期货行情、K线看盘、AI 盯盘、期货晨报时触发。
version: "1.0.0"
icon: "📦"
metadata:
  source: "https://github.com/lilaosanxueai/futures-analyzer"
  installed_by: "github-installer"
  installed_at: "2026-08-25T21:00:15.036656"
---

# 📦 期货实时分析助手（本地 Web 应用）

> 由 [github-installer](https://github.com) 自动安装自 [lilaosanxueai/futures-analyzer](https://github.com/lilaosanxueai/futures-analyzer)。

## 简介

国内期货准实时行情看板 + AI 分析对话的本地 Web 应用：AkShare 数据源（约 3~5 秒延迟），支持 K 线蜡烛图（MA/MACD/RSI/KDJ/BOLL 技术信号）、价格预警、AI 盯盘、主题要闻、AI 晨报、交易心得（可同步飞书云文档）。AI 对话支持智谱 GLM 与 DeepSeek，**支持发送图片给视觉模型**（DeepSeek `deepseek-v4-flash-vision-exp` / 智谱 `glm-4v-flash`）。

## 使用方法

```bash
# 启动（二选一）
cd C:\Users\10166\.agents\skills\futures-analyzer
.venv\Scripts\python.exe app.py     # 使用已装好的虚拟环境
# 或双击 start.bat

# 浏览器打开
# http://127.0.0.1:8300
```

- 启动后页面立即可开（预热在后台进行）；首次行情/日线数据就绪约需 1 分钟，之后每 5 秒自动刷新行情
- AI 对话：页面右上角「⚙ AI 设置」填入智谱（open.bigmodel.cn，glm-4-flash 免费）或 DeepSeek（platform.deepseek.com）的 API Key
- **图片对话**：输入框支持 📎 选择 / Ctrl+V 粘贴截图 / 拖拽三种方式附图（自动压缩到最大边 1568px JPEG，单条最多 4 张），需在 AI 设置中选用视觉模型（DeepSeek 填 `deepseek-v4-flash-vision-exp`）
- 链路自检：访问 `http://127.0.0.1:8300/?selftest=1`
- 注意：仅供研究学习，不构成投资建议，无下单功能

## 安装来源

- **GitHub**: [lilaosanxueai/futures-analyzer](https://github.com/lilaosanxueai/futures-analyzer)
- **版本**: main@d1505c9
- **许可**: （仓库未声明）
- **语言**: Python 3.12（FastAPI + AkShare + pandas）
- **本地路径**: `C:\Users\10166\.agents\skills\futures-analyzer`
- **虚拟环境**: `.venv`（依赖已安装：akshare 1.18.94 / fastapi 0.141.1 / pandas 3.0.5 / uvicorn 0.52.4 / pypinyin 0.55.0）

## 本地修复记录（安装时）

1. **`call_ak` 线程绑定修复**（app.py）：原代码用 `asyncio.to_thread` 将 AkShare 调用分发到线程池的不同线程，py_mini_racer 的 V8 实例跨线程复用会挂起（表现为启动预热卡死、目录接口空）。已改为所有调用固定走单线程 `ThreadPoolExecutor`，验证通过。
2. **`get_directory` 异常日志**（app.py）：目录刷新失败原先静默 `pass`，现打印失败原因到控制台。
3. 已知特性：首次启动预热需 1~3 分钟（新浪接口逐品种匹配主力合约），之后目录缓存 1 小时。

## 本地修复记录（对比分析功能排查）

1. **`call_ak` 超时 + 重试 + 换线程**（app.py）：新浪接口偶发断连（RemoteDisconnected）或无限挂起，单个挂起请求会堵死串行执行队列，导致对比分析/相关性请求挂起 100 秒以上无响应。现每个调用 60 秒超时、失败自动重试 2 次（1s/2s 退避）、超时或连接错误时丢弃旧线程换新线程。
2. **`get_directory` 失败退避**（app.py）：目录拉取失败原先不缓存失败状态，行情每 5 秒轮询都会触发全量目录拉取（内部为 82 品种逐个匹配请求），失败场景下每分钟近千次请求轰炸新浪导致限流恶性循环（接口返回 HTML 反爬页 → JSONDecodeError）。现失败后 5 分钟内不再重试。

## 本地功能增强（v0825d）

1. **AI 对话图片功能**：前端输入框支持 📎 选择 / Ctrl+V 粘贴截图 / 拖拽附图，canvas 自动压缩（最大边 1568px、JPEG 0.85、PNG 透明铺白底），待发预览可删除，气泡缩略图点击放大；后端 `ChatMessage` 增加 `images` 字段并构造 OpenAI 多模态 `content` 数组（`image_url` data URL），历史消息图片自动剥除仅保留最新一条（省 token）；模型下拉建议含 `deepseek-v4-flash-vision-exp`（DeepSeek 视觉）与 `glm-4v-flash`（智谱视觉）。已实测 DeepSeek 视觉模型带图调用成功。
2. **图片功能加固**：粘贴监听升级为 document 级（页面任意位置 Ctrl+V 截图即附图，纯文本粘贴不受影响）；发图时若当前模型名不含 vision/4v/vl 等特征则 toast 提醒换视觉模型；后端拒绝单张 >12MB base64（413）与总数 >8 张（400）的请求。全视图（工作台/详情/对比/要闻/心得）回归通过，无脚本错误。

## 本地性能优化（v0825d+）

1. **启动预热后台化**：原 `lifespan` 中 `await _warmup()` 阻塞端口绑定 1~3 分钟（目录接口内部 82 品种串行匹配）。改为 `create_task` 后台预热后**端口 1 秒就绪、页面 0.05 秒可开**，数据在后台慢慢热（前端本就有加载态）。安全性依据：单线程 executor 已保证所有 AkShare 调用（含 V8 首次初始化）串行，预热不再承担串行初始化职责。
2. **已收盘日线长缓存**：最后交易日早于今天的日线数据缓存 TTL 从 60 秒延长到 30 分钟（数据已不再变化），大幅减少非交易时段对新浪的重复请求（正是此前限流的诱因之一）。实测二次 K 线请求 0.02 秒（缓存命中，首次 1.36 秒）。
3. 清理死代码：移除失去读取方的 `_warmed` 全局变量与过时注释。

## 本地功能移除（v0825e）

- **对比分析功能已整体移除**（应用户要求）：删除了前端导航按钮、对比视图 HTML（比价/价差/归一化图表、统计通道、相关性矩阵）、全部 JS 逻辑与样式，以及后端 `/api/compare`、`/api/correlation` 接口与 `_aligned_closes` 辅助函数。导航现为 5 个视图（工作台/合约详情/资讯要闻/交易纪律/交易心得），快捷键 Alt+1~5。AI 对话中用文字请 AI 对比品种的能力不受影响。

## 本地新增功能（v0825f）：交易纪律 · 开仓前检查

把"风险参数 + 铁律 + 开仓前检查清单 + 交易许可单"的纪律体系落成规则引擎（源自用户与 AI 的提示词讨论）：

- **13 项规则**（R001~R013）：趋势方向（日线收盘 vs MA20 + MA20 三日斜率；趋势反转早期可凭自评"≥2 条反转确认"豁免，价格在错误一侧则无条件禁止）、入场信号（同向技术信号 ≥2 条，逆势需自评反转确认，附 ATR14 波动力度参考）、自选品种池、核心矛盾自评、梯度建仓间距（对加仓，与最近同向未平仓价差 ≥ 阈值）、加仓次数上限、日内止损线、日/周交易次数、许可单五项（自评+表单校验）、冲动冷静期（自评 + 距上笔申请间隔）、风险收益比 + 单笔风险上限（填该笔风险 % 时校验）、多时间框架共振（60 分钟 MA20 方向与日线一致，数据缺失跳过仅警告）。
- **判定分级**：致命违反任一即「禁止开仓」，严重违反仅警告（R011 冷静期、R013 共振为严重级）。
- **数据自动判定**：行情/趋势/信号直接用 AkShare 日线与现有 indicators 计算；次数/盈亏/加仓从交易日志统计。
- **交易日志**：`discipline_log.json`（已加 .gitignore）记录每笔申请与裁决，平仓回填盈亏后自动统计今日/本周次数与日内盈亏。
- **API**：`POST /api/discipline/check`（检查并记录）、`GET /api/discipline/log`、`POST /api/discipline/settle`（平仓回填）、`GET|POST /api/discipline/config`（风控参数，存 config.json 的 discipline 节）。
- **AI 审查**：一键把裁决明细交给 AI，要求补充规则引擎覆盖不到的维度（盘口/消息面/情绪）。
- 已实测（v0825g 二次打磨）：场景 A（做多 + 自评反转确认，价格在 MA20 上方但斜率未顺）R001 豁免通过、ATR14 显示正常、R013 60 分钟共振一致 → 允许开仓；场景 B（risk_pct=2% 超上限 1%）R012 违反 → 禁止；场景 C（做空但价格在 MA20 错误侧 + 反转确认勾选）R001 仍违反 → 纯抄底被堵死；修复了上一版 R011 在无日志时的 `gap_min` NameError 边界 bug。

## 行为增强（v0825h，参考 TraderSync/Edgewonk/海龟法则）

1. **ATR 头寸建议（海龟 1N 法则）**：参数面板新增「账户权益」；检查结果自动计算"止损=1×ATR14 口径"的建议手数（每手风险 = ATR×合约乘数，内置约 40 个常见品种乘数表，未收录品种不显示）。实测：RB0 ATR14=26.2 → 每手风险 262 元 → 20 万账户×1% = 2000 元预算 → 建议 ≤7 手。
2. **情绪标签（行为归因）**：申请时选择情绪（冷静/犹豫/FOMO/报复），随日志沉淀；统计条自动画像"哪种情绪下你最容易违反纪律"（该情绪 ≥2 笔且有被拦时显示）。
3. **连续纪律天数**：被拒绝=系统拦截成功不断链；只有"通过但带违反（无视警告硬开）"才断链——游戏化激励坚持。
4. **持仓浮动盈亏**：日志表格对持仓中记录用页面实时行情算浮动点数（红绿显示），复用 5 秒轮询零额外请求。
5. AI 审查提示词同步带上情绪与头寸建议。

## 复盘闭环（v0825i）

1. **纪律周报**：日志区「📋 纪律周报」按钮——收集近 7 天日志拼成提示词走 `/api/ai/chat`，在纪律页内以 Markdown 渲染 AI 复盘报告（概览/被拦规则分布/情绪被拦率分析/执行质量/下周可执行建议，教练口吻）。空日志时显示引导文案。实测 624 字报告：FOMO 情绪被拦率 100% vs 冷静 0% 的归因表格正确生成。
2. **平仓行内输入**：日志表「平仓」按钮由原生 prompt 弹窗改为行内输入（±%，回车或 ✓ 提交，Esc 取消）。

## 交易记录增强（v0825j）

1. **实际 R 倍数**：平仓时可选填出场价，引擎自动计算实际 R（(出场-入场)/计划风险，方向调整）——衡量"计划盈亏比兑现了多少"。表格新增「计划比 / 实际R」两列对照。
2. **深度统计卡**：已平笔数、胜率、累计已实现盈亏%、平均盈利/亏损%、最大单笔亏、平均 R（有出场价记录时）。
3. **记录管理**：筛选 tab（全部/持仓/已平仓/被拒）；每行可删除（confirm 确认，`POST /api/discipline/delete`）；已平仓记录可点「改」重填盈亏与出场价。
4. **备注可见**：许可单交易理由以 📝 悬浮提示显示在行内（title）。
5. **CSV 导出**：一键导出全部记录（含 BOM，Excel 中文不乱码），16 列完整字段。

## AI 全权判定架构（v0825k）

**移除全部自评勾选**（反转确认/品种认知/许可单/冷静期），主观判断 100% 由 AI 基于行情数据与交易理由判定，并新增 AI 决策否决权：

- **两阶段流程**：① 客观规则（趋势基础/信号计数/自选池/间距/加仓/日周次数/日内止损/盈亏比/60 分钟共振——数据判定）；② AI 审查（市场上下文 + 指标信号 + 客观判定摘要 + 交易计划 → `_llm_json` 结构化判定）。
- **AI 判定四项**：`reversal_confirmed`（反转 ≥2 条客观依据，供 R001 豁免与 R002）、`knows_variety`（R004 品种认知：理由是否含具体供需/技术逻辑与价位）、`impulse_detected`（R011 冲动检测：综合情绪自报与理由文本）、`decision_correct`（**R014 决策评估，致命级否决权**：计划整体是否值得执行，含置信度与 80 字评估）。
- 规则升级为 14 条；AI 评审结论（含评估摘要）写入日志 `ai_review` 字段，CSV 导出与纪律周报均携带。
- **推理型模型适配**：`_llm_json` 按模型给足 max_tokens（deepseek 视觉推理模型思维链耗 token 大，1200 会截断正文返回空 JSON）、400 自动降级、空响应给出明确诊断提示；temperature=0.2 稳定格式。
- AI 不可用时不放行（明确报错重试，不降级回自评——自评已按需求移除）。
- **实测两场景对照**：A「合理计划但埋瑕疵」——AI 抓出止损 15 点 < ATR 25.5（易被噪音扫损）且目标位非真实前高，判定"计划不值得执行"（正确识别硬伤）；B「FOMO 糟糕计划」——AI 判定认知=False/冲动=True/决策=False（高置信度），R004/R011/R014 全违反 + 4 项致命禁止。AI 交叉验证数据的能力超出预期。

## UI 皮肤系统（v0825m）

1. **4 套主题**：深夜蓝（默认）/ 纯黑（OLED 省电高对比）/ 浅色（白天看盘，红绿加深适配白底）/ 墨绿（长时间盯盘护眼）。顶栏「🎨」按钮弹出主题色卡（渐变预览），点击即切、localStorage 持久化、`<head>` 内联脚本防闪烁恢复。
2. **颜色全面变量化**：散落的硬编码色（选中行深蓝、白色标题、代码块底色、滚动条、红涨绿跌、琥珀强调）全部收敛为 CSS 变量（--selected/--text-strong/--code-bg 等 16 个），主题定义区外零硬编码。
3. **视觉细节**：细圆角滚动条（hover 高亮）、全局背景/文字/边框 0.18s 过渡、按钮按压缩放反馈。
4. 实现说明：主题通过 `html[data-theme]` 属性切换 CSS 变量组；红涨绿跌语义色随主题微调（浅色主题用更深红绿保证白底可读）。

## AI 交易分析深度增强（v0825l）：持仓陪伴 + 事后校验闭环

1. **持仓 AI 体检**（`POST /api/discipline/holding-review`）：持仓行「🩺」按钮——原计划 + 当前行情 + 指标 + 当初 AI 评审 → 判定 continue/reduce/exit + 执行偏差 + 建议止损 + 生死价位。实测：浮盈 36 点时 AI 建议减仓（RSI6 82.3/KDJ J 104.2 超买、破布林上轨），并指出"浮盈未上移止损"的执行偏差。
2. **平仓 AI 单笔复盘**（`POST /api/discipline/trade-review`）：已平行「复盘」按钮——计划 vs 实际对照（执行评级 A-D、是否按计划、实际 R vs 计划 R）+ 校验 AI 当初判定（正确/偏差）+ 一条教训。
3. **AI 判定准确率统计**（stats.perf）：`ai_approved`（AI 认可且已平的胜率/均盈亏）+ `ai_rejected_hypothetical` 反事实验证（AI 否决的单若执行按当前价的假想点数，负=拦得值）。实测：被 AI 否决的追高单反事实 -54 点。
4. **分析上下文注入持仓**：`_build_market_context` 自动附带该品种当前持仓（入场/止损/目标/理由），AI 对话分析会兼顾持仓风险而非仅给方向观点。

## 实时解读（v0825n）：实时数据 → AI 盘中快评

1. **`GET /api/ai/realtime?symbol=X`**：强制拉最新行情（绕过缓存）+ 买一卖一盘口 + 日内分时结构（开高低时间/均价偏离/量能分布/尾盘动向）+ 30 分钟线近 12 根 + 日线指标信号 + 60 分钟方向 + 品种要闻 + 用户持仓 → `_llm_text` 生成 300 字内盘中快评（日内节奏/多空倾向/关键价位/风险提示，有持仓则兼顾应对）。同品种 5 分钟缓存（force=1 强刷，缓存命中 0.002s）。
2. **前端**：AI 对话区顶部「⚡ 实时解读」面板——一键按当前选中合约生成；「自动·15m」开关（localStorage 记忆，仅工作台可见时刷新省 token）；快评以 Markdown 渲染，标注数据时间与最新价。
3. 实测：RB0 快评引用全部真实数据（日内高低点及时间、RSI6 82.35、布林上轨 3071.39），观点明确（震荡偏多但短线过热）、价位具体。

## 实时数据接入纪律板块（v0825o）

1. **共用实时快照**：抽取 `_realtime_snapshot(symbol)`（现价/盘口/日内分时结构/30 分钟节奏/60m 方向/日线指标/要闻/持仓），实时解读与纪律 AI 审查共享。
2. **纪律 AI 审查注入实时盘面**：check 的 AI prompt 新增【实时盘面】段——现价与计划入场的偏离 %、买一卖一盘口、日内结构、30m 节奏。实测威力：AI 用实时 MA10（3044.3）当场证伪交易理由中"止损 3050 在 MA10 下方"的陈述（实际在其上方），判定"陈述与数据不符，不执行"。
3. **表单现价辅助**：纪律表单品种行实时显示现价（优先用 5 秒轮询缓存，非自选品种按需拉取），「填入」按钮一键把现价填入入场价。

## 上游整合（v0825q，上游 9 新提交研究后择优合并）

上游 dfd545d 相比本地安装基线新增 9 个提交。对比决策：**剔除重合**（交易日志与复盘统计——本地纪律日志更强；开仓评估面板——本地 14 条规则+AI 全权判定更强；默认模型切换——已配置）；**整合 6 项**：

1. **三层品种消息面引擎**（上游 dfd545d）：`_variety_news_deep`（东财品种深研，10 分钟缓存，50+ 品种搜索词）+ `_shmet_news`（上海有色网金属快讯，90 秒缓存，金属/能源品种全量注入、其他品种宏观词过滤）+ 原快讯流，三层注入 `_build_market_context`。修正了上游 `_variety_search_word` 中 `asyncio.run` 在事件循环内会崩的 bug。实测：RB0 返回真实产业新闻（上期所注册公告）、CU0 返回 SHMET 实时快讯。
2. **AI 语义筛选资讯**（上游 bdd7573）：噪音词扩充 26 词 + AI 批量判定相关性并标 11 类标签（10 条一块、失败对半重试、降级回规则过滤）；前端 AI 徽章、类别标签、"只看相关"开关（localStorage）。实测链路通（67 条→5 条判定相关）；当前视觉推理模型判定质量一般（有误判），默认仅排序不过滤。
3. **飞书 webhook 主动推送**（上游 18f7652）：`_feishu_push` + 盯盘异动（含 AI 解读）推群 + `report_push_loop` 晨/夜报定时后台生成并推送（9 点/21 点后自动，不依赖打开页面；修正了上游 due 条件的分钟判断 bug）+ 设置弹窗 webhook 配置与一键测试 + `/api/feishu/push-test`。
4. **AI 对话历史存档**（上游 38d03d2）：对话自动存 localStorage（60 条），刷新恢复最近 20 条；心得页「💬 对话历史」查看/复制/清空/☁ 导出飞书独立《AI 对话记录》文档（`/api/chat-export`）。
5. **皮肤 +2**（上游 d60ece1 借鉴）：玻璃·夜（毛玻璃 backdrop-filter + 渐变光斑）与极光（流动极光动画，respect prefers-reduced-motion），共 6 套。
6. **AI 调用自动重试**：`_llm_text_retry`（502/429 时空响应自动重试一次），实时解读与语义筛选已接入。

## Token 优化补遗（v0826e）

补齐上轮审计盲区（持仓体检/单笔复盘本就轻量无需动），修掉两个残余浪费：

1. **chat 轻量模式**：`ChatIn.light=1` 跳过 `_build_market_context`。**纪律周报**（走 /api/ai/chat 且 symbol=null，此前白背自选行情块）与**自检**（`?selftest=1` 只验链路却注入约 1200 字行情）改走 light——system 仅剩 268 字 SYSTEM_PROMPT，省约 80%/次。
2. **「分析当前合约」按钮指令去重**：四层权重说明与 SYSTEM_PROMPT 逐字重复，压为一句引用（230→120 字），语义不变（权重在 system 里逐字保留）。

## Token 消耗再优化（v0826d，应用层降量）

零 LLM 调用的构建器实测（导入 app 模块直接量 prompt 体积）定位大头后逐项瘦身：

| 调用点 | 优化前 | 优化后 | 手段 |
|---|---|---|---|
| 实时解读 prompt | ~1414 字 | ~1029 字（-27%） | m30 逐根 OHLC（479字）→ 收盘序列+区间（94字） |
| 纪律 AI 审查 prompt | ~2334 字 | ~1949 字（-16%） | 同上（共用快照） |
| chat system | 393+1225 | 268+1196 | SYSTEM_PROMPT 精简 32%；日线去"开"列 |
| chat 历史窗口 | 20 条/旧截200字 | 14 条/旧截120字 | 前端 sendChat（近4条仍全量） |
| 晨报要闻 | 20 条 | 12 条 | _generate_report |
| 视觉图片 | 1568px | 1120px | 面积 -49%，视觉模型按分辨率计 token |

注意：盯盘 `_call_ai_simple` 默认 max_tokens=2048 **不可压小**——推理模型思维链先烧额度，压小即重现空响应。质量回归：SC0 综合分析 2310 字，资金面权重优先/具体数值引用/换月提醒全保留。

## 全盘梳理（v0826c）

静态扫描（幽灵 id/孤儿函数/版本残留/路由清单）+ 实测冒烟（四视图 + 零控制台报错）后修掉三处：

1. **对话存档撑爆 localStorage**：`pushMsg` 存档时把 base64 图片（单条最多 4 张、每张数百 KB）一并写入 `fa_chat_history`，几张图即超 5MB 配额 → setItem 抛错被静默吞掉 → **存档从此静默停止更新**（对话历史/AI 复盘对话源双双断粮）。修法：存档时剥离 images 只存 {role,content,ts,sym}。
2. **错误提示污染三处**：`pushMsg("assistant", "调用失败…", "error")` 以 assistant 角色进入——AI 上下文（浪费 token 且误导模型）、本地存档、AI 复盘语料。修法：改推 `role:"error"`（渲染不变，三处过滤器自动排除）。
3. **死代码清理**：`renderLines`（SVG 折线图，K 线图替代后零调用）、`loadSparkline`（自注释空壳）删除；对话历史框/飞书导出补显示时间与品种（存档已有 ts/sym）。

## AI 复盘分析（v0826b）

应用户要求：AI 对话与交易心得按已有记录生成针对性复盘报告，可灵活选时间段、品种。

1. **`POST /api/ai/review`**：前端按筛选条件过滤后传 chats（{role,content,ts,sym}，截 60 条：问≤100 字/答≤380 字）+ notes（截 50 条 content≤220 字）→ 组装六段式复盘 prompt（品种观点演化/判断质量验证/行为模式/重复性问题/保留做法/改进建议，要求引用原句、禁止臆造存档外行情）→ `_llm_text_retry` 生成 Markdown 报告。
2. **对话存档补 ts/sym**：`pushMsg` 落 `Date.now()` 与当前选中品种（此前只有 role/content，无法按时间/品种筛）。旧存档无 ts 的条目仅在「全部时间」档纳入；无 sym 的按内容关键词匹配品种。
3. **心得页新增「🧠 AI 复盘」按钮 + 弹窗**：数据源勾选（AI 对话/交易心得）、时间段（7/30/90 天/全部/自定义起止）、品种（逗号分隔+datalist 候选，前缀匹配 SC→SC0）、实时统计预览「命中对话 X 条、心得 Y 条」；报告渲染复用 renderMarkdown。
4. **顺带修复 `_llm_text` 空响应**：max_tokens 下限从硬编码 1600 改为 `max_output_for(model)`（deepseek 系 8192）——与 `_llm_json` 对齐；此前 deepseek-v4-flash 思维链耗尽 1600 token 必返回空，实时解读等所有 `_llm_text` 调用方一并受益。
5. 实测：真实生成 1519 字报告，AI 对单条记录如实说明局限、引用原句、不编造。

## UI 修复：深色主题下按钮白块（v0825z）

用户截图反馈纪律页「现价」旁有刺眼白块。根因：`.btn` 基础类只设 `color: var(--text-strong)`（深色主题=纯白）却未设 background——Windows Chrome 原生按钮底色是 buttonface 浅灰 rgb(240,240,240)，白字白底文字隐形、按钮成白块。13 处裸 `.btn small-btn` 全部受害（填入/保存参数/纪律周报/CSV/对话历史/测试/平仓/🩺/改/复盘/复制/清空）。

1. `.btn` 基础类补 `background: var(--panel-2); color: var(--text); border: 1px solid var(--border)`——六主题全部可读。
2. `.btn.primary` 显式 `color:#fff`；glass/aurora 两套亮主色主题单独配深字 `#0b1020`（原继承 --text 在亮底上仅 1.16 对比度）。
3. 六主题全部补 `color-scheme`（dark×5、light×1）——Windows Chrome 原生控件（下拉弹出列表/数字输入步进箭头/滚动条）此前一直按浅色渲染。
4. 验证：Playwright 逐主题读取计算样式算 WCAG 对比度，6 主题 × 7 按钮（含 primary/ghost/accent/裸 btn）全部 ≥3:1，多数 10+。注意：藏在收起 `<details>` 内的元素（如 btnDcSave）getComputedStyle 可能返回未重算的旧值，测前须 `details.open=true` + 强制 reflow。

## UI 修复：纪律页重叠遮挡（v0825y）

用户截图反馈「交易纪律 · 开仓检查」页存在重叠遮挡。根因是 grid 列的经典溢出：`.disc-grid` 左列固定 320px 但未设 `min-width:0`，grid 项默认 `min-width:auto` 不肯收缩——品种下拉选项文字长（含国际盘 24H 后缀），其内容最小宽度把表单列撑出 320px 轨道，视觉上压到右栏提示文字上；品种行内「现价」标签同样被挤重叠。

1. `.disc-grid` 改 `minmax(0, 340px) minmax(0, 1fr)`，并给 `.disc-form/.disc-result` 加 `min-width:0`——列可收缩，溢出消除。
2. 全局 `.form-row input/select` 加 `min-width:0`（设置弹窗等同款问题一并修复）。
3. 品种行内层 flex 加 `flex-wrap:wrap`、下拉加 `min-width:120px`——极窄时「现价/填入」换行而非重叠。
4. 「运行开仓检查」按钮加 `margin-top:12px` 与说明文字拉开间距。
5. Playwright 三宽度（1280/860/2000px）包围盒几何检测全部通过：无交叠、无横向滚动。
6. 后续（0826a，应用户要求）：左列 340→420px 加宽、列距 16→18px、单列断点 900→980px。

## 国际品种全链路支持（v0825x）

打通国际品种（WTI/BRENT/GOLD/DXY）与全应用体系的断层——此前纪律检查/资金情绪/详情页仅支持国内品种：

1. **`get_daily` 自动分流**：国际品种走新浪外盘历史接口（映射 WTI→CL、BRENT→OIL、GOLD→GC，长缓存），字段对齐国内格式——**纪律检查 14 条、资金情绪、技术指标、K 线日K、晨报全部自动兼容**，零下游改动。
2. **`fetch_quote` 国际分支**：从实时快照组装 quote（无盘口/量仓），实时解读/对话对国际品种正常工作。
3. **`fund_sentiment` 动量降级**：外盘数据源无持仓量/成交量 → 自动切换纯价格动量因子（5 日/20 日动量）并标注；国内品种仍用完整价量仓八状态。
4. **友好降级**：K 线分钟周期/分时图对国际品种返回明确提示（"仅支持日 K"/"无日内分时"）。
5. **前端**：详情页与纪律检查下拉新增「国际盘 24H」分组（WTI/BRENT/GOLD/DXY）。实测：WTI 日 K（MA/BOLL 正确）+ 资金情绪（+60，5 日动量 +5.58%）全通。

## API Token 节省优化（v0825u）

系统性排查全部 LLM 调用点后的瘦身（保持模型不变，纯上下文/流程优化）：

1. **纪律检查去重**（最大头）：此前 AI 审查同时注入完整市场上下文（10 天 OHLC 明细/基本面框架/中期统计/自选行情表）+ 实时快照，两者高度重复。现只用实时快照并补充指标一行与宏观要闻，单次审查省约 50-60% 输入 token。
2. **持仓体检同理**：完整上下文 → 轻量实时快照五行（现价/日内结构/指标/60m 方向/宏观要闻）。
3. **`_build_market_context` 瘦身**（对话/实时解读用）：日线明细 10 天→5 天（指标已概括趋势）；自选行情表改为"选中品种一行详细 + 其余一行摘要"；宏观要闻 8→5 条。
4. **对话历史裁剪**（前端）：发送前最近 4 条完整、更早消息每条截断 200 字，避免长回复反复全量重发。
5. **实时解读自动模式防空转**：周末休市或选中品种最新价与上次解读时完全一致 → 跳过调用（待机状态显示在 rtSym），消除休市时段与数据不变时的无端消耗。
6. **质量回归**：精简后纪律检查反而更精准——AI 用现价 3169 vs 计划入场 3060 算出"需先跌 3.4% 且跌破 MA60，逆势逻辑；目标 3120 低于现价，盈亏比虚高不切实际"（high 置信度否决），证明砍掉的是冗余而非有效信息。

## 聚焦国际盘改造（v0825t，应用户要求砍掉综合资讯）

1. **资讯获取大幅收缩**：删除三层消息面引擎（东财深研/SHMET）、AI 语义筛选、股市噪音词、主题体系。`news()` 只保留新浪全球快讯单源 + **严格白名单过滤**：仅保留【特朗普发言/动作】（特朗普/白宫/关税/财政部…）与【中东重大动向】（伊朗/以色列/空袭/霍尔木兹/胡塞/OPEC…）两类，其余快讯全部丢弃。实测：海量快讯流 → 精准剩 2 条真实中东要闻。
2. **新增 `/api/intl` 国际盘实时接口**：新浪 hq 字面量 URL 单请求获取四品种（WTI=hf_CL / 布伦特=hf_OIL / COMEX黄金=hf_GC / 美元指数=DINIW），30 秒缓存，秒级实时。
3. **AI 盯盘切换**：监控对象从自选国内品种改为固定四国际品种（自建采样队列对比 5 分钟前价格，阈值 WTI/布伦特 1%、黄金 0.6%、美元指数 0.3% × 灵敏度），异动仍走 AI 解读 + 飞书推送。
4. **晨报适配**：要闻段=特朗普/中东；品种快照段=国际盘四品种（含高低价）。
5. **前端**：要闻视图改为「🌍 国际盘监控」——四品种报价大卡（红涨绿跌 + 高低）+ 宏观要闻列表（🇺🇸特朗普 / 🌍中东 标签，关键词高亮）；停留页面时报价每 5 秒跟随轮询刷新。
6. 另：laolixueai.github.io 主页提交已按用户要求 revert 并推送（仓库恢复为仅 README）。

## 夜盘时段修正（v0825v+，用户指正 SC/AU 交易至凌晨 2:30）

1. **分品种夜盘三档**：SC/AU/AG 至 02:30；金属类 CU/AL/ZN/PB/NI/SN/SS/BC/AO 至 01:00；其余有夜盘品种至 23:00；苹果/红枣/鸡蛋/生猪/花生无夜盘。
2. **跨午夜归属**：凌晨 0:00-收盘属前一交易日夜盘（周六凌晨=周五夜盘延续；周日凌晨无夜盘；周日凌晨无夜盘）。
3. **`domestic_session_active(prefix)` 分品种判断**：日盘全品种 + 夜盘按档；monitor_loop 国内轨道逐品种门控。
4. **`is_trading_time()` 总开关扩至 02:30**（market_open 状态展示同步修正）。
5. 单元验证 14/14 通过（含 23:30 螺纹已收/原油仍交易、02:31 黄金已收、周六凌晨延续、周日凌晨无夜盘等边界）。

## 上游整合第二批（v0825r）：TradingView 交互 + 图形标注

1. **K 线缩放平移**（上游 14ce111）：滚轮缩放（20~500 根，rAF 节流）、拖拽平移看历史、双击复位；数据加载扩到 500 根（`_kfull` 全量 + 窗口化渲染）。
2. **K 线叠加开关**：MA / BOLL 独立开关（后端 kline 新增 boll 三轨，实测 RB0 上轨 3182/中轨 3062/下轨 2942）。
3. **Ctrl+K 命令面板**：合约代码/名称/拼音搜索直达详情（自动加入自选），内置 8 条命令（视图/晨报/皮肤/设置），↑↓ 导航回车执行（适配本地视图：纪律/皮肤按钮）。
4. **标签页标题实时价格**：多标签盯盘时直接看标题（`RB0 3112 ↑0.5% | 期货助手`），预警闪烁时自动让位。
5. **日内走势图形标注**（上游 cb02f31）：5 种标注（📈多/📉空/⚠️风险/📏价位线/📝批注），数据坐标存储（刷新/缩放不丢位）、按合约+日期 localStorage 持久化；点击放置（prompt 输入文字）、双击删除、🧹清当日；🤖AI 逐条评估标注依据与冲突并给独立推演；💾一键转结构化心得（可同步飞书）；`?annot_test=1` 自测钩子（实测 3 个示例标注渲染正确）。
6. **switchView 保留 URL 参数**（上游修复）：view 切换不再丢失 annot_test/selftest 等参数。
7. 修复：版本引用断链（app.js 引用停在 0825l，已统一 0825r）。

## 全卷审查收尾（v0825s）

1. **HTML 转义补全**：renderDcVerdict（AI 计划/评估/反转依据）、dcTradeReview（总评/教训）等全部 AI 文本插入 innerHTML 处统一 `esc()` 转义——AI 输出常含 `<`/`>`（如"收盘 < MA10"），此前会截断 HTML 结构。
2. **晨报接入持仓与纪律**：`_generate_report` 新增【当前持仓】（入场/止损/目标/实时浮动，提醒今日关注止损位）与【交易纪律状态】（连续天数/次数/今日盈亏/被拒统计与 top 违反规则，AI 会针对性提醒规避）。
3. **盯盘覆盖持仓**：monitor_loop 监控集并入纪律日志中全部 open 持仓品种——即使品种移出自选，持仓异动仍被盯住并推送。
4. **纪律页浮动盈亏实时刷新**：行情 5 秒轮询后若在纪律页且有持仓行，重渲染浮动列（编辑行内输入时跳过防打断）。

## 表单全客观化（v0825p）

**移除全部主观勾选与输入**（加仓勾选、交易理由 textarea、情绪下拉、该笔风险 % 输入），表单只剩 5 个纯客观参数（品种/方向/入场价/止损价/目标价）：

1. **加仓自动判定**：日志存在同品种同方向未平仓记录 → 自动视为加仓（R005 间距/R006 次数随之判定）。
2. **交易计划 AI 生成**：AI 基于实时盘面代写许可单第 5 项——核心矛盾、关键价位（具体数字）、入场逻辑定位，存入 note 并在结果区展示。R004 相应改为"AI 判定该品种当前是否存在与计划一致的可交易逻辑"。
3. **情绪 AI 推断**：`detected_mood`（从计划特征客观推断：追价/短间隔/亏损后申请→fomo/revenge；结构匹配→calm），替代情绪自报，情绪画像统计延续。
4. **单笔风险自动评估**：按 ATR 1N 建议仓位自动算风险占比并对上限校验（替代手算 risk_pct 输入）。
5. 实测：仅提交 5 参数 → AI 推断冷静、生成"中期偏多短线超买，回调至 MA5(3068) 做多、损 MA10 下方"的完整计划、评估"值得执行"。

---

📦 *由 github-installer v1.0.0 自动生成*
